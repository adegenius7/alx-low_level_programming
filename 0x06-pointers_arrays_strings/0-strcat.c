#include "main.h"
#include <stdio.h>

/**
 * *_strcat - check the code
 *@dest: destination string
 *@src: source string
 * Return: Always 0.
 */
char *_strcat(char *dest, char *src)
{
	char *dest, dest[], src[];

	char *src;

	int i, j;

	for (i = 0; i <= dest; i++)
	{
		dest[dest + i] = src[i]
	}
	return (dest);
}
