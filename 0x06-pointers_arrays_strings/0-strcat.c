#include "main.h"
#include <stdio.h>

/**
 * *_strcat - check the code
 *@dest: destination string
 *@src: source string
 * Return: Always 0.
 */
char *_strcat(char *dest, char *src)
{
	char *dest, dest[], src[];

	char *src;

	int i, j;

	len1 = _strlen(dest);

	len2 = _strlen(src);

	for (i = 0; i <= len2; i++)
	{
		dest[len1 + i] = src[i]
	}
	return (dest);
}
