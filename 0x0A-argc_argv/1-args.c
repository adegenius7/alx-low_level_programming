#include <stdio.h>

/**
 *main- progarm starts here
 *@argc: number of commands
 *@argv: place holder for commands
 *Return: 0
 */

int main(int argc, char *argv[] __attribute__((unused)))
{
		printf("%d\n", (argc - 1));

	return (0);
}
